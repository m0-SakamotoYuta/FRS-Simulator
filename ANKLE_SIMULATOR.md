# ankle simulator — 使い方ガイド

足関節 (脛骨・距骨・他 N本) の接触動態を、**RealSense D405** + **ArUco マーカー**で追跡・可視化するシステム。

---

## 目次
1. [全体像](#1-全体像)
2. [必要環境と起動](#2-必要環境と起動)
3. [⓪ D405 ライブ撮影](#3--d405-ライブ撮影)
4. [① 初期状態スキャン](#4--初期状態スキャン)
5. [② 計測データ / ArUco設定](#5--計測データ--aruco設定)
6. [③ 骨リスト](#6--骨リスト)
7. [🔨 マーカー準備 (印刷)](#7--マーカー準備-印刷)
8. [④ ArUco検出 + PnP](#8--aruco検出--pnp-6dof姿勢時系列)
9. [⑤ 可視化 + アニメ + ヒートマップ](#9--可視化--アニメ--ヒートマップ)
10. [マーカー-骨キャリブレーション (任意)](#10-マーカー-骨キャリブレーション-任意)
11. [試験タブ](#11-試験タブ複数試験の並行管理)
12. [トラブルシューティング](#12-トラブルシューティング)
13. [スキャナ無しでの追跡検証](#13-スキャナ無しでの追跡動作検証)
14. [データ形式](#14-データ形式)
15. [実験フローとUIセクションの対応](#15-実験フローとuiセクションの対応)
16. [数値目安](#16-数値目安)
17. [既知の制限](#17-既知の制限)

---

## 1. 全体像

```
【事前準備】             【実験当日】               【解析】
┌──────────┐          ┌──────────┐          ┌──────────┐
│ ②③ 設定 │          │ ① 初期   │          │ ③ 骨モデル│
│ ↓        │          │  スキャン │          │  +位置合わせ│
│ 🔨 印刷  │  →→→→→→ │ ⓪ D405   │  →→→→→→ │ ④ 検出   │
│ ↓        │          │  録画    │          │ ⑤ アニメ  │
│ 装着     │          └──────────┘          │  +ヒート  │
└──────────┘                                └──────────┘
```

**座標系**:
- **C** カメラ座標系 (D405)
- **W** ワールド座標系 = 初期スキャン座標系
- **Lᵢ** 骨 i のローカル (試験後個別スキャン)
- **Mkᵢ** マーカー i の座標系

**時刻 t での骨 i の W系姿勢:**
```
T_W←bone_i(t) = ΔMkᵢ(t) · T_W←Lᵢ
   ここで  ΔMkᵢ(t) = T_C←Mkᵢ(t) · T_C←Mkᵢ(0)⁻¹
```

骨とマーカーは剛結合前提なので、マーカーの相対運動をそのまま骨に適用できます。

---

## 2. 必要環境と起動

- Windows + Python venv (`.venv/`) : `opencv-contrib-python 5.0`, `pyrealsense2 2.58`, `open3d`, `pyvista`, `numpy`, `matplotlib`
- Intel RealSense D405 (USB 3.0 接続)
- レーザープリンタ + マット紙

起動:
```bash
"C:/Users/yutas/Desktop/FRS-Simulator/.venv/Scripts/python.exe" "C:/Users/yutas/Desktop/FRS-Simulator/FRS-SIMULATOR.py"
```
タブ「**ankle simulator**」を選択。

---

## 3. ⓪ D405 ライブ撮影

D405で色+深度を録画し、SDK ネイティブ形式 `.db3` (pyrealsense2 2.58+) で保存します。

### 3.1 接続確認
1. D405 を **USB 3.0 ポート** に接続
2. **「接続確認」** ボタン → デバイス情報 (シリアル・ファームウェア・内部パラメータ・depth scale) が表示されれば正常

### 3.2 撮影パラメータ

| 項目 | 推奨値 | 説明 |
|---|---|---|
| 解像度@fps | `1280x720@15` | 精度と帯域のバランスが良い |
| 開始前 破棄フレーム | 45 (既定) | 露光/AE安定待ち |
| 深度手動露光 | OFF (既定) | 通常は自動でOK |

### 3.3 撮影手順

1. **「プレビュー+録画 (.db3保存)」** をクリック
2. 保存先ダイアログ (既定: `cache/ankle_d405_YYYYMMDD_HHMMSS.db3`)
3. 右上に **コントロールパネル** が現れる、色/深度プレビューが2窓
4. マーカーが写る位置にカメラを構える (**推奨距離 20-40cm**)
5. パネルの **「録画開始 (SPACE)」** ボタン → 破棄フレーム待ち → 録画開始
6. 対象を動かす
7. **「録画停止 (SPACE)」** ボタン → 完了ダイアログ
8. `.db3` パスが自動で「② 深度データ」にセット

### 3.4 操作
コントロールパネル・cv2動画ウィンドウ・キーボードのどれからでも操作可能:

| アクション | 方法 |
|---|---|
| 録画開始 / 停止 | パネルのボタン (最も確実) / SPACE (パネル or cv2ウィンドウがフォーカス時) |
| キャンセル / 中止 | パネルの「キャンセル」ボタン / ESC / cv2ウィンドウの × |

### 3.5 撮影のコツ

- 中心距離 **20-40cm** を保つ (画面中央の十字十字マークに表示)
- 照明: 均一で明るく (蛍光灯/LED)、直射日光・鏡面反射は避ける
- マーカーが常時視野内に入るように
- 動きは滑らかに (急峻すぎるとブラーで検出失敗)

---

## 4. ① 初期状態スキャン

全骨がピン+マーカー装着済みの状態を3Dスキャンした STL/OBJ ファイル。

**用途:** Stage 2 の位置合わせ先、Stage 5 の背景表示

**⓪〜④は無くても動作します。** Stage 5 で骨アニメとヒートマップを見たい時に必要。

---

## 5. ② 計測データ / ArUco設定

| 入力 | 説明 |
|---|---|
| RGBビデオ | mp4/avi (⓪で撮影した場合は不要) |
| 深度データ | `.db3` (D405用) / `.bag` (旧SDK) / npz など |
| カメラ内部パラメータ | `.db3`/`.bag` 使用時は自動抽出 |
| **ArUco辞書** | **`DICT_4X4_50`** 推奨 (0-49 の50個、印刷サイズ小) |
| **マーカー実寸 (mm)** | 印刷後にノギスで実測した値 |

**マーカー実寸は超重要**: プリンタスケール誤差 (1-3%) がそのまま PnP スケール誤差になるので、必ずノギスで測って反映してください。

---

## 6. ③ 骨リスト

**骨ごとに1枚の ArUco マーカーを対応付ける**。骨数は可変。

各骨の設定:
| 項目 | 説明 |
|---|---|
| 骨名 | 例: `tibia` / `talus` / `calcaneus` |
| ArUco ID | 印刷したマーカーのIDと**一致必須** |
| 表示色 | Stage 5 アニメ用 |
| 骨モデル | 試験後スキャン (STL/OBJ/PLY) |
| 初期スキャン側 骨領域 | ①のうちこの骨の領域 |
| 位置合わせ方式 | RANSAC / 主軸PCA / 手動3点 |
| スケール補正 | スキャナとCTでスケール差ある時ON |

**位置合わせパラメータ (タブ共通)**: RANSAC距離/反復/信頼度, ICP距離/反復, サンプル点数, ボクセルサイズ

**実行ボタン**:
- 個別実行 (骨エディタ内「初期スキャンへ位置合わせ」)
- 全骨バッチ (「全骨を一括位置合わせ」)
- 結果プレビュー (「位置合わせ結果を確認」)

---

## 7. 🔨 マーカー準備 (印刷)

**実験の事前準備セクション**。③でIDを登録し②で辞書/実寸を設定した後にここでファイルを生成します。

### 7.1 出力形式

2種類:

#### 🟢 **「マーカー印刷用PDF (実寸・推奨)」** ← 実試験ではこちら

- A4 に **物理実寸で正確配置**、複数マーカー / ページ (20mmなら42個/ページ)
- ラベル (`ID=0 (test1) 20.0mm`) + 切り取り目安の破線枠
- **印刷手順:**
  1. Adobe Acrobat Reader / Chrome / Edge で PDF を開く
  2. 印刷設定で **「実際のサイズ」または「100% スケール」**
  3. ❌「ページに合わせる」「フィット」は絶対にOFF
  4. マット紙にレーザー印刷
  5. ノギスで実測 → ② の値に反映
  6. quiet zone (外枠内の白余白) は残す

#### 🟡 **「マーカー画像を保存 (PNG)」**

- 個別のPNGファイル (800×800px, quiet zone 100px 付き)
- ディスプレイでのプレビュー用途、または他ツールで加工したい時
- Windowsフォトから印刷すると「ページ全体」の既定で拡大されるので、**実試験では PDF ボタンを使うこと**

### 7.2 印刷後の作業

1. マーカーをノギスで実測 → 誤差があれば ② の「マーカー実寸(mm)」を更新
2. 骨に電動ネジ + ボーンセメント + アクリル板固定 → マーカーシール貼付
3. 装着完了後、①で初期状態を3Dスキャン
4. ⓪で撮影に進む

---

## 8. ④ ArUco検出 + PnP (6DOF姿勢時系列)

### 8.1 検出パラメータ

| 項目 | 意味 |
|---|---|
| フレーム間引き | 1=全フレーム, 2=1つおき, ... |
| 参照フレーム t=0 | Stage 5 のアニメ基準フレーム番号 |

### 8.2 検出実行

**「ArUco検出+PnP実行」** ボタン → 進捗ダイアログ表示

内部処理:
1. cv2.aruco.detectMarkers でコーナー検出 + サブピクセル精緻化
2. `SOLVEPNP_IPPE` で平面マーカーの両姿勢解 (表裏 flip ambiguity)
3. 深度画像で表裏判別 (各解の予測深度 vs 実測深度の RMSE で選択)

完了後にステータス表示:
```
検出完了: 250 フレーム / ソース=xxx.db3
  ID=0: 検出率 95.2%, 再投影誤差(中央値)=0.31px
  ID=1: 検出率 89.7%, 再投影誤差(中央値)=0.42px
```

### 8.3 結果の保存・読込・可視化

| ボタン | 用途 |
|---|---|
| **姿勢時系列を保存** (`.npz`) | 長期保存・再解析用 |
| **姿勢時系列を読込** | 既存の`.npz`を再ロード |
| **マーカー軌跡を可視化 (骨モデル不要)** | カメラ座標系での3D軌跡表示 (追跡動作確認用) |

---

## 9. ⑤ 可視化 + アニメ + ヒートマップ

### 9.1 静的可視化
- **初期状態を可視化** — ①のスキャンのみ
- **全骨を可視化** — ③で位置合わせ済みの全骨を W系配置 (+ 初期スキャン半透明背景)

### 9.2 アニメーション
- **シミュレーション実行** — 姿勢時系列で全骨をアニメ表示
  - フレームスライダー
  - 各骨は色付きメッシュ、`SetUserMatrix` で高速更新
  - 情報テキスト: `Frame t/N (t=XX.Xs)`

### 9.3 骨対ヒートマップ
- **ヒートマップ対象 骨A / 骨B** を Combobox で選択
- 選択された骨ペア間の signed distance を Open3D BVH で計算
- LUT (固定 -10〜0mm): 接触=緑 / めり込み=赤 / 離間=**透明**
- フレーム毎に遅延キャッシュ

---

## 10. マーカー-骨キャリブレーション (任意)

**現状の Stage 5 アニメでは不要** (骨-マーカー剛結合前提)。ただし将来必要:
- 骨座標系での運動量表示
- 複数マーカーで冗長トラッキング

**手順** (骨エディタ「マーカー-骨キャリブ」ボタン):
1. 骨モデル (試験後スキャン) が PyVista で開く
2. マーカー4隅を **TL → TR → BR → BL** の順にクリック
3. Umeyama/Kabsch で `T_L←Mk` 算出
4. RMSE で品質評価 (< 0.5mm=極めて良好, < 1.0mm=良好)
5. 確認可視化 (骨 + ピック点 + 復元マーカー + Mk座標軸)

---

## 11. 試験タブ(複数試験の並行管理)

タブバー上の「試験1」ボタンで**独立した試験**を切替:
- 入力・骨リスト・位置合わせ結果・キャッシュがタブ独立
- **＋** 新規タブ (末尾タブから複製、④の入力だけ既定値に戻す)
- **右クリック** 名前変更・削除・並べ替え
- **ドラッグ** 並べ替え

状態は `frs2015_gui_state_ankle_sim.json` に自動保存。

---

## 12. トラブルシューティング

| 症状 | 原因 | 対処 |
|---|---|---|
| D405接続で「起動失敗」 | 別アプリがカメラ使用中 / USB 2.0 | 他アプリ終了 / USB 3.0ポートへ |
| **印刷したマーカーが用紙いっぱい** | PDFビューアの「ページに合わせる」設定 | **「実際のサイズ / 100%」で印刷** |
| SPACE/ESC無反応 | パネル非フォーカス | パネル自体をクリックしてからキー |
| cv2ウィンドウ × で復活 | (修正済み) | 最新版なら `getWindowProperty` で検出 |
| 検出率 < 50% | マーカーが小/光沢/ボケ | 距離短縮 / 照明改善 / マット素材 |
| 姿勢が瞬間ジャンプ | 平面表裏判別失敗 (正面近く) | マーカーを 10-30°傾けて撮影 |
| 位置合わせでfitness低 | 領域の対応部位が違う / スケール差 | 手動3点で概略指定 / スケール補正ON |
| ヒートマップが赤一色 | メッシュが完全にめり込み | 位置合わせを再確認 |

---

## 13. スキャナ無しでの追跡動作検証

**⓪〜④ (撮影→検出→軌跡) は骨モデルもスキャンも無しで動作します**。詳細な手順は [ANKLE_TEST_PROCEDURE.md](ANKLE_TEST_PROCEDURE.md) を参照。

要点:
1. マーカー印刷用PDFで実寸マーカーを印刷
2. 段ボール/木片などに貼付
3. D405で5-10秒撮影しながら手で動かす
4. ArUco検出実行
5. **「マーカー軌跡を可視化」** で3D軌跡表示・検出率確認

判定基準: **検出率 > 80% + 軌跡が滑らか** ならOK。

---

## 14. データ形式

- **姿勢時系列 (.npz)**:
  - `id_<N>_poses` (N,4,4) 4x4同次変換 T_C←Mk per frame
  - `id_<N>_detected` (N,) bool
  - `id_<N>_reproj_err` (N,) float
  - `timestamps` (N,) float
  - `meta_json` (intrinsics, marker_size_mm, aruco_dict, source, ...)

- **状態ファイル**: `frs2015_gui_state_ankle_sim.json` (試験タブごとに入力・骨リスト・位置合わせ結果)

- **プリセット**: `frs2015_knee_reg_presets.json` (位置合わせパラメータ、knee共通)

- **録画データ**: RealSense `.db3` (SDK 2.58+ 標準) / 旧形式 `.bag` も読込可

---

## 15. 実験フローとUIセクションの対応

実際の試験は **3つのフェーズ** に分かれます:

### 事前準備 (実験の数日〜数週間前)
| # | 作業 | UI |
|---|---|---|
| A-1 | 骨リストにID登録 | ③ |
| A-2 | ArUco辞書・マーカー実寸を設定 | ② |
| A-3 | **マーカーPDF出力 → 印刷 → 実寸確認** | **🔨** |
| A-4 | 骨に電動ネジ + ボーンセメント + アクリル + マーカーシール装着 | (物理) |

### 実験当日
| # | 作業 | UI |
|---|---|---|
| B-1 | 装着完了後の3Dスキャン | ① |
| B-2 | FRS-2015 ロボットに骨をセット (物理) | - |
| B-3 | D405 カメラ設置 (物理) | - |
| B-4 | **D405 で背屈/底屈を録画** | **⓪** |

### 実験後 (解析)
| # | 作業 | UI |
|---|---|---|
| C-1 | 解剖して骨を個別3Dスキャン | (物理) |
| C-2 | 骨モデルを ③ に登録 | ③ |
| C-3 | 位置合わせ実行 | ③ |
| C-4 | **ArUco検出+PnP実行** | **④** |
| C-5 | (任意) マーカー-骨キャリブ | 骨エディタ |
| C-6 | アニメ+ヒートマップ | ⑤ |

**UIの並び (⓪→①→②→③→🔨→④→⑤) は実験フローと一致するように並んでいます。**
各セッションは一直線に進めます (準備セッションは ②③🔨 → 実験セッションは ①⓪ → 解析セッションは ③④⑤)。

---

## 16. 数値目安

- D405 20cm距離、20mmマーカー、正常検出時: **並進 σ < 0.5mm、回転 σ < 0.3°** 想定
- 合成データ検証: 姿勢復元 R誤差 < 1°, t誤差 < 2mm (小傾き5°は表裏判別限界)
- マーカー-骨キャリブ: 0.5mmピックノイズで R誤差 1.1°, RMSE 0.36mm

---

## 17. 既知の制限

- 現状 Stage 5 では `marker_to_bone_T` (Stage 4) を使用していない (骨-マーカー剛結合前提)
- 未検出フレームは直前姿勢を保持 (補間は将来対応)
- `SOLVEPNP_IPPE_SQUARE` は cv2 5.0.0 で不具合があり `SOLVEPNP_IPPE` を使用中
- pyrealsense2 2.58+ は `.bag` を拒否し `.db3` を要求 (両方読込可能)
- Open3D の日本語パス問題は `_knee_load_o3d_pcd` (pv.read経由) で回避済み
