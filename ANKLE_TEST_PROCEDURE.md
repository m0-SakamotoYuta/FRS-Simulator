# ankle simulator — 追跡テスト手順 (スキャナ不要)

**目的:** 3Dスキャナや実際の骨を使わずに、ArUco + D405 での追跡が正常に動くか検証する。

**所要時間:** 15-30分 (印刷含む)

**必要なもの:**
- Windows PC + FRS-Simulator インストール済み
- Intel RealSense D405 (USB 3.0接続)
- レーザープリンタ (マット紙 A4 1枚)
- ノギス (印刷実寸測定用)
- 段ボール片 or 木片 (剛体台紙、3〜4枚)
- カッター + はさみ

---

## Phase 0: 準備 (5分)

### 0.1 アプリ起動

```bash
"C:/Users/yutas/Desktop/FRS-Simulator/.venv/Scripts/python.exe" "C:/Users/yutas/Desktop/FRS-Simulator/FRS-SIMULATOR.py"
```

タブ「**ankle simulator**」を開く。

### 0.2 骨リストにテスト用の項目を追加 (③)

- 骨リストの **「＋追加」** を3〜4回押す
- 各骨をクリックして選択、以下を設定 (適用ボタンを押す):

| # | 骨名 | ArUco ID |
|---|---|---|
| 1 | `test1` | 0 |
| 2 | `test2` | 1 |
| 3 | `test3` | 2 |
| 4 | `test4` | 3 |

☑ ArUco ID は必ず **重複しない整数** で

### 0.3 マーカー実寸を設定 (②)

- **ArUco辞書**: `DICT_4X4_50` (既定)
- **マーカー実寸 (mm)**: とりあえず `20.0` を入力 (印刷後に実測値へ差し替え予定)

---

## Phase 1: マーカー印刷 (5-10分)

### 1.1 PDF 生成

- ④ セクションの **「マーカー印刷用PDF (実寸・推奨)」** ボタンをクリック
- 保存先ダイアログ: `aruco_markers_20mm_DICT_4X4_50.pdf` などで保存
- 完了ダイアログに配置枚数と印刷手順が表示される

### 1.2 印刷

- 生成した PDF を **Adobe Acrobat Reader / Chrome / Edge** で開く
- 印刷設定:
  - ✅ **「実際のサイズ」** (Acrobat) または **カスタムスケール「100」** (Chrome/Edge)
  - ❌ 「ページに合わせる」「フィット」「用紙サイズに縮小」は OFF
  - 用紙: **A4 マット紙** (光沢紙 NG)
  - プリンタ: レーザー推奨

### 1.3 実寸確認 (重要)

- 印刷したマーカーの **一辺をノギスで測る** (黒枠のいちばん外側から外側まで)
- 期待: 20.0 mm
- 実測値が 19.8 mm や 20.3 mm 等の誤差なら、その値を ② の「マーカー実寸(mm)」に反映

例: 実測 20.15mm → ② のマーカー実寸 (mm) に `20.15` と入力

### 1.4 切り出し + 貼付

- 破線に沿って、または少し外側でカット (quiet zone = 外側の白余白は必ず残す)
- 4枚のマーカー (ID=0, 1, 2, 3) を、それぞれ独立した段ボール片/木片に貼付
  - 剛体で、平面部分がマーカーサイズ以上あればOK
  - スプレー糊 or 両面テープで剥がれないように

---

## Phase 2: 撮影 (2分)

### 2.1 D405 接続確認

- ⓪ の **「接続確認」** ボタン
- 期待: デバイス情報ダイアログ (SN, FW, 内部パラメータ表示)
- 失敗する場合はケーブル/USBポート変更

### 2.2 プレビュー + 録画

1. ⓪ の **「プレビュー+録画 (.db3保存)」** をクリック
2. 保存先: 既定名でOK (`cache/ankle_d405_YYYYMMDD_HHMMSS.db3`)
3. 右上に **コントロールパネル**、色/深度プレビューが2窓
4. カメラを構え、**4枚のマーカーが全部見える** 位置に手を置く
   - **推奨距離 25-35cm** (画面中央十字の "X.XXX m" 表記で確認)
5. パネルの **「録画開始 (SPACE)」** を押す
   - 「露光安定待ち…」→「録画中 (Nフレーム)」に切替
6. **5-10秒間、マーカーを動かす**:
   - 上下左右に並進
   - 回転 (ロール・ピッチ・ヨー混ぜて)
   - カメラに近づけたり遠ざけたり
   - **急峻な動きは避ける** (ゆっくり、なめらかに)
7. パネルの **「録画停止 (SPACE)」** を押す
8. 完了ダイアログ (「N フレームを保存しました」)

### 2.3 自動連携確認

- 「② 深度データ」に `.db3` のパスが自動セットされているか確認

---

## Phase 3: 検出 (1-2分)

### 3.1 検出パラメータ

- ④ の **「フレーム間引き」**: `1` (全フレーム処理)
- **「参照フレーム t=0」**: `0` (今回は使わないので0のままでOK)

### 3.2 検出実行

- ④ の **「ArUco検出+PnP実行」** をクリック
- 進捗ダイアログが表示される (キャンセル可)
- 完了後、青色のステータス表示に結果:

```
検出完了: 150 フレーム / ソース=ankle_d405_XXXX.db3
  ID=0: 検出率 95.2%, 再投影誤差(中央値)=0.31px
  ID=1: 検出率 89.7%, 再投影誤差(中央値)=0.42px
  ID=2: 検出率 91.4%, 再投影誤差(中央値)=0.38px
  ID=3: 検出率 87.6%, 再投影誤差(中央値)=0.45px
```

### 3.3 検出率の判定

| 検出率 | 判定 |
|---|---|
| ≥ 90% | ◎ 極めて良好 |
| 80-90% | ○ 良好 |
| 50-80% | △ 使えるが要改善 (照明・距離・マーカー品質) |
| < 50% | ✗ 要見直し (下記参照) |

| 再投影誤差(中央値) | 判定 |
|---|---|
| < 0.3 px | ◎ 光学系優秀 |
| 0.3-0.5 px | ○ 良好 |
| > 1 px | △ 要見直し |

### 3.4 検出率が低いときのチェック

- マーカーが全部同時に画面内にあったか
- 光沢反射でマーカーが白飛びしていないか (プレビュー再確認)
- マーカー実寸 (②) と印刷実寸が一致しているか
- モーションブラーで潰れていないか (ゆっくり動かす)
- 距離が近すぎ (< 15cm) or 遠すぎ (> 60cm) ないか

---

## Phase 4: 軌跡可視化 (1分)

### 4.1 3D軌跡表示

- ④ の **「マーカー軌跡を可視化 (骨モデル不要)」** をクリック
- PyVistaウィンドウが開く:
  - **原点 (0,0,0)**: 黒球 + RGB軸 = カメラ位置
  - **各マーカーの軌跡**: 骨リストの色で線描画
  - **始点**: 小さいスフィア
  - **終点**: 大きいスフィア + ラベル
  - **左上テキスト**: 各マーカーの検出率と移動範囲(mm)

### 4.2 判定

| 見え方 | 意味 | アクション |
|---|---|---|
| 軌跡が滑らかで連続 | ◎ 追跡OK | 実試験へ進めてOK |
| 軌跡がジャンプ/跳ねる | 表裏判別失敗 (静止に近い) | もっと傾けて撮影 |
| 途中で軌跡が消える | 遮蔽 or 画面外 | マーカーを画面内に |
| 全く軌跡が出ない | ID不一致 or 未検出 | 印刷IDと骨リストID再確認 |
| 縮尺がおかしい | マーカー実寸ズレ | ②の実寸を再測定・修正 |

### 4.3 定量チェック (剛体貼付ならでは)

もし複数マーカーを **同じ段ボール** に貼付していれば、マーカー間の距離は時間不変です。マウス操作でPyVista上の点間距離を測ったり、`.npz` を Python で読んで距離時系列の標準偏差を確認できます。

期待: 距離の SD < 1mm (追跡誤差の RMS 目安)

---

## Phase 5: 記録と考察 (2-3分)

以下を記録:

- [ ] 撮影距離 (画面中央十字の表示): 平均 ___ cm
- [ ] 照明条件: ___
- [ ] 撮影時間: ___ 秒
- [ ] 総フレーム数: ___
- [ ] マーカー実寸 (印刷後実測): ___ mm
- [ ] 各マーカーの検出率:
  - ID=0: __%
  - ID=1: __%
  - ID=2: __%
  - ID=3: __%
- [ ] 再投影誤差(中央値): __ px
- [ ] 軌跡評価: (◎○△✗)
- [ ] マーカー間距離 SD (同一台紙に貼った場合): __ mm

---

## 実試験に進む前のチェックリスト

- [ ] 全マーカーの検出率が **80% 以上**
- [ ] 再投影誤差中央値が **0.5px 未満**
- [ ] 軌跡が滑らかで大きな飛びがない
- [ ] マーカー実寸 (印刷実測値) が ② に反映済み
- [ ] マーカー印刷品質 (エッジがくっきり、光沢無し)
- [ ] D405 との作業距離が確保できる場所

これら全部OKなら、実際の骨・電動ネジ・ボーンセメントでの装着に進んで問題なし。

---

## トラブル別対処表

| 症状 | 原因 | 対処 |
|---|---|---|
| 印刷したマーカーが用紙いっぱいに | PDFを「ページに合わせる」で印刷 | 「実際のサイズ / 100%」で再印刷 |
| D405接続失敗 | 別アプリ (RealSense Viewer等) が掴んでる | 他アプリ終了 → USB 3.0再接続 |
| 「録画開始」でクラッシュ | 旧SDK版で `.bag` 拡張子 | 最新コードで `.db3` 使用 |
| SPACE/ESCが効かない | tk メインウィンドウにフォーカス | コントロールパネル自体をクリック |
| 検出率が全マーカーで0% | ID or 辞書の不一致 | ③のIDと辞書、印刷したPDFの辞書を再確認 |
| 一部マーカーだけ検出率低 | そのマーカーが遮蔽 or 傾きすぎ | 撮影構図を見直し |
| 軌跡が瞬間的にジャンプ | 平面表裏判別の限界 | マーカーを 10-30° 傾けて撮影 |
| 距離が実際と大きく違う | マーカー実寸ズレ | ノギスで測って ② に反映 |

---

## 次のステップ (実試験へ)

このテストがOKなら:

1. **骨に電動ネジ + ボーンセメントでアクリル板固定**
2. アクリル板にマーカーシール貼付
3. 装着後、静止状態で 10秒撮影 → 検出 → **静的姿勢ノイズ σ_R** を実測 (目標 < 0.3°)
4. スキャナで初期状態スキャン + 個別骨スキャン
5. FRS-Simulator の ① / ③ にスキャンデータを取り込み
6. Stage 2 で位置合わせ (`T_W←Lᵢ`)
7. 実試験録画 → 検出 → Stage 5 でアニメ + ヒートマップ

詳細は [ANKLE_SIMULATOR.md](ANKLE_SIMULATOR.md) を参照。
