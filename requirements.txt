# FRS Simulator - 依存パッケージ一覧
# Usage: pip install -r requirements.txt

# --- 3Dビジュアライゼーション ---
numpy>=1.21
pyvista>=0.38
open3d>=0.17
vtk>=9.0

# --- データ処理 ---
scipy>=1.9
openpyxl>=3.0
pandas>=1.5

# --- FEMレポート生成 ---
python-docx>=0.8.11
matplotlib>=3.5

# --- ドラッグ&ドロップ（任意。未導入でもファイル選択は可能） ---
tkinterdnd2>=0.3.0

# --- GPU高速化（任意。未導入ならCPUで従来どおり動作） ---
# 解析サーバー(Ubuntu + NVIDIA)では CUDA版を入れると、ヒートマップ計算をGPUで高速化:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# CPUのみ(Windows/Mac ノート等)は不要（キャッシュ表示のみ）。入れる場合:
#   pip install torch
torch  # optional