# FRS Simulator - 依存パッケージ一覧
# Usage: pip install -r requirements.txt

# --- 3Dビジュアライゼーション ---
numpy>=1.21
pyvista>=0.38
open3d>=0.17
vtk>=9.0

# --- データ処理 ---
scipy>=1.9
openpyxl>=3.0
pandas>=1.5

# --- FEMレポート生成 ---
python-docx>=0.8.11
matplotlib>=3.5

# --- ドラッグ&ドロップ（任意。未導入でもファイル選択は可能） ---
tkinterdnd2>=0.3.0

# --- 高速ヒートマップ計算 ---
# ヒートマップ(符号付き距離)は Open3D の BVH(RaycastingScene) で高速計算する。
# open3d は上で導入済みなので、追加インストールは不要（GPU/torch も不要）。
#
# ※ torch は現状ヒートマップには使用しない（BVHの方が総当たりGPUより桁違いに速いため）。
#    必要な場合のみ任意で:  pip install torch

# --- ankle simulator（ArUcoマーカートラッキング）---
# ArUco検出+PnPには cv2.aruco (contrib版) が必要 (Stage 3以降)
opencv-contrib-python>=4.7
# Intel RealSense .bag ファイルを直接読む場合に必要 (D400/D405など)
pyrealsense2>=2.55